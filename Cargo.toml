[package]
name = "miri"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }  # Command-line parsing
logos = "0.15.0"

[dev-dependencies]
assert_cmd = "2.0"
