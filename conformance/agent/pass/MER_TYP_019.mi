// summary: Accepted counterpart of MER_TYP_019: GPU Launch Block Dimensions Invalid does not fire.
use system.gpu

gpu fn my_kernel()
    let x = 1

fn main()
    my_kernel().launch(Dim3(1, 1, 1), Dim3(16, 16, 1))
