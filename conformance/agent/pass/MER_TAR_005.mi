// summary: Accepted counterpart of MER_TAR_005: Invalid GPU Parallel Construct does not fire.
use system.gpu

fn main()
    gpu let arr = [0, 1, 2, 3]
    let sum = arr.reduce(0, fn(acc i32, x i32) i32: acc + x)
