## Rule

A name given to `miri skill show` or `miri skill install` is not one this build carries. `miri skill list` names every skill it does carry.

## Before

```sh
miri skill show nonexistent
```

## After

```sh
miri skill show miri-lang
```

## Reference

[Build and Command Line](../reference/build.md)
