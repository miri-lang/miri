// SPDX-License-Identifier: Apache-2.0
// INCORRECT: Type-checker error — match on enum Color is missing the Blue variant.
// Level: Type checker
// Expected error: Non-exhaustive match
use system.io

enum Color
    Red
    Green
    Blue

fn main()
    let c = Color.Red
    let name = match c
        Color.Red: "red"
        Color.Green: "green"
    println(name)
